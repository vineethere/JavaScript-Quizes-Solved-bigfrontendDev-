// https://bigfrontend.dev/quiz/Function-call

function a() {
  console.log(1);
  return {
    a: function() {
      console.log(2);
      return a();
    }
  };
}

a().a();



// Step-by-step Execution:
// The first call is a().
// Inside a(), it logs 1.
// Then it returns an object with a method a.
// So a().a() becomes:
// First, a() runs → logs 1 → returns an object like { a: function() { ... } }
// Then .a() is called on that returned object:
// Inside this method, it logs 2.
// Then it calls a() again.
// In the second call to a() (inside .a()):
// Logs 1 again.
// Returns a new object.

// Output:
// 1
// 2
// 1

